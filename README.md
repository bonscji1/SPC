# SPC

Smart Pig's Cookbook — a recipe and portion calculator.

## Run

From the repository root (Docker required):

```bash
docker compose up --build
```

Open http://localhost:8080

That is the supported way to run the published stack. Compose will grow to include backend and databases; today it starts the frontend.
