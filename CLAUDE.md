# Claude — SPC

This file is the entry point for Claude when working in this repository.

## Start here

1. Read **`AGENTS.md`** at the repository root for shared rules and layout.
2. If your task is in **`frontend/`**, also read `frontend/AGENTS.md` and `frontend/docs/`.
3. If your task is in **`backend/`**, also read `backend/AGENTS.md` and `backend/docs/`.
4. For cross-cutting context, see `docs/README.md`.
5. For in-progress or planned work, check `plans/`.

## Project

**Smart Pig's Cookbook (SPC)** — monorepo with frontend and backend subprojects.

Human-oriented overview: `README.md`
